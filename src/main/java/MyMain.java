import java.util.*;
import java.util.stream.Collectors;

public class MyMain {

    // Return true if the input is bigger than 10, and false otherwise
    public static boolean isBiggerThanTen(int num) {
        return true;
    }

    // Return "negative" if the number is less than zero, and "positive"
    // if it is greater than or equal to zero
    public static String getSign(int num) {
        return "";
    }

    // Return whichever number is closer to 5, or a if they are equidistant
    public static int closerTo5(int a, int b) {
        return 0;
    }

    // Return "small" if the number is less than 10, "medium" if the number
    // is from 10-99, and "large" if the number is at least 100.
    // You may want to use two ternary operators for this!
    public static String getSize(int num) {
        return "";
    }
}